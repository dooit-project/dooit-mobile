# 화면 캡처 기준

`docs/screenshots`는 README와 화면 가이드에서 사용하는 현재 UI의 대표 캡처다.

## 최신 캡처

- 촬영일: **2026-09-13**
- 기준 커밋: `c133220`
- 환경: Expo Web · mock API · Chrome · 390×844 · light theme
- 시간대: `Asia/Seoul`
- 캡처 도구: 기존 검증 세션의 Chrome 확장 연결 재사용. 내장 브라우저는 연결 불가로 복구 지침·목록 확인 후 Chrome으로 전환한 상태
- 상세 상태: [`manifest.json`](./manifest.json)

## 갱신 규칙

- 대표 캡처는 촬영일로부터 **7일째가 되기 전에** 갱신한다. 촬영 후 7일 이상 지나면 UI 변경이 없어도 다시 확인하고 촬영일을 갱신한다.
- 화면 구조, 하단 탭, 문구, 아이콘, 여백 또는 주요 데이터 상태가 바뀌면 7일을 기다리지 않고 즉시 다시 촬영한다.
- 캡처를 바꿀 때 PNG와 `manifest.json`의 촬영일, 기준 커밋, 환경, 화면 상태를 한 변경으로 갱신한다.
- `npm run docs:check`는 누락된 파일, manifest 밖의 PNG, 잘못된 날짜, 7일 초과를 검사한다.
- 캡처에는 mock 데이터만 사용하며 실제 이메일, 일정, 검색어 등 개인정보를 넣지 않는다.
- 마켓용 편집본은 대표 캡처가 바뀐 뒤 다시 export하고 [`APP_STORE_ASSETS.md`](../marketing/APP_STORE_ASSETS.md)에 export 날짜를 기록한다.

## Audit 캡처

`docs/audits`의 이미지는 특정 검증 시점의 증거이므로 같은 파일을 덮어쓰지 않는다. 각 audit README에 촬영일, 기준 커밋, 환경과 viewport를 기록한다. 최신 근거로 계속 참조할 audit이 7일을 넘으면 새 날짜 디렉터리에 다시 캡처하고 기존 자료는 과거 기록으로 취급한다.

## 2026-09-13 CI 복구 검증

- 실패 실행: [34697839492](https://github.com/dooit-project/dooit-mobile/actions/runs/34697839492)
- 원인: 2026-09-12 실행에서 9월 5일 캡처가 7일이 되어 `docs:check`의 freshness 검사 실패. 9월 11일 로컬 검증 이후 푸시까지 날짜 경계를 넘었다.
- 조치: `c133220`의 실제 mock Web 화면 14개를 Chrome 확장으로 새로 촬영하고 직접 확인했다. 촬영일·기준 커밋·화면 상태를 함께 갱신했다.
- Today: mock Task 1개, 완료 0개에서 완료 영역이 숨겨진 상태를 확인했다. 상세는 해당 Task를 열어 촬영했다.
- Calendar·완료 기록·목표·오늘 계획은 빈 상태, 검색은 입력 전 상태다. 경로 재진입 시 mock 데이터가 초기화되므로 이전 데모 데이터가 있는 캡처로 설명하지 않는다.
- 긴 Calendar·더보기·Task 상세는 첫 viewport만 촬영했다. 스크롤 아래 전체 내용과 native 접근성 판정은 이번 캡처 갱신 범위에 포함하지 않는다.
- 관찰된 console error: 없음. 실제 계정 입력·회원가입·재설정 메일 발송은 수행하지 않았다.
- 마켓 편집 이미지는 이번 대표 캡처로 다시 export하지 않았으며 기존 초안으로 유지한다.
